# makeopts
<% @makeopts.each do |makeopt| -%>
<%= makeopt %>
<% end -%>

<% @pkg_makeopts.sort.each do |pkg, pkg_makeopts| -%>
# pkg_makeopts for <%= pkg %>
.if ${.CURDIR}=="/usr/ports/<%= pkg %>"
<% pkg_makeopts.each do |makeopt| -%>
<%= makeopt %>
<% end -%>
.endif

<% end %>
